import { Container } from "react-bootstrap";
import TableTop from './TableTop/TableTop';

const MainApp = () => {
  return (
    <Container fluid>
      <TableTop />
    </Container>
  );
};

export default MainApp;
