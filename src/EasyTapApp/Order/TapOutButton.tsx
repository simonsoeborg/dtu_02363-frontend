import { Button } from "react-bootstrap";
import "../../resources/Css/layout.css";

const TapOutButton = () => {
  return (
    <Button className="button-tabOut" variant="outline-primary">
      Tap Out
    </Button>
  );
};

export default TapOutButton;
